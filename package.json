{
  "scripts": {
    "start:api": "npm --prefix fullstack-app/backend start",
    "start:ui": "npm --prefix fullstack-app/frontend start",
    "start:all": "npm run start:api & npm run start:ui",
    "test": "npm run start:api & npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "c8": "^10.1.3",
    "istanbul-lib-coverage": "^3.2.2",
    "istanbul-lib-report": "^3.0.1",
    "istanbul-reports": "^3.1.7",
    "typescript": "^5.0.0",
    "v8-to-istanbul": "^9.3.0",
    "wait-on": "^8.0.4"
  },
  "prepare": "npx playwright install"
}
